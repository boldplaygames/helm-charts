apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kpi-service.fullname" . }}-test-connection"
  labels:
    {{- include "kpi-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kpi-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
